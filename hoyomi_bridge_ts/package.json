{
  "name": "hoyomi_bridge_ts",
  "module": "index.js",
  "types": "index.d.ts",
  "type": "module",
  "scripts": {
    "build": "rm -f index.d.ts && rm -f index.js && bun build index.ts --outfile=index.js && tsup index.ts --dts-resolve --dts-only -d ./ --format esm",
    "postinstall": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16",
    "tsup": "^8.5.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
